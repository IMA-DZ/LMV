language: csharp
solution: LibreMetaverse.sln

matrix:
  include:
    - mono: latest
    
deploy:
  provider: pages
  skip_cleanup: true
  github_token: $GITHUB_TOKEN # Set in travis-ci.org dashboard
  on:
    branch: master    
